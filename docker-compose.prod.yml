services:
  postgres:
    image: postgres:16-alpine
    container_name: utn_bd_postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: utn_bd
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d utn_bd"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: ./app
    container_name: utn_bd_app
    environment:
      DB_HOST: postgres
      DB_NAME: utn_bd
      DB_USER: readonly
      DB_PASS: ${READONLY_PASSWORD}
      DB_ADMIN_USER: admin
      DB_ADMIN_PASS: ${POSTGRES_PASSWORD}
    ports:
      - "3579:5000"
    depends_on:
      postgres:
        condition: service_healthy
    restart: always
    networks:
      - dokploy-network
      - default

networks:
  dokploy-network:
    external: true

volumes:
  postgres_data:
